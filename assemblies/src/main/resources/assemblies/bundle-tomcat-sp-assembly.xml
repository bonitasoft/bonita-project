<assembly xmlns="http://maven.apache.org/ASSEMBLY/2.2.0"
          xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
          xsi:schemaLocation="http://maven.apache.org/ASSEMBLY/2.2.0 https://maven.apache.org/xsd/assembly-2.2.0.xsd">
    <id>${bonita.environment:lowercase}-bundle</id>
    <formats>
        <format>zip</format>
    </formats>
    <includeBaseDirectory>false</includeBaseDirectory>
    <baseDirectory>${project.build.directory}/package-bundle-tomcat-sp</baseDirectory>
    <dependencySets>
        <!-- Unpack Tomcat Bundle artifact -->
        <dependencySet>
            <unpack>true</unpack>
            <useStrictFiltering>true</useStrictFiltering>
            <includes>
                <include>com.bonitasoft.distrib:bundle-tomcat-sp</include>
            </includes>
        </dependencySet>
        <!-- Copy application archive into bundle classpath -->
        <dependencySet>
            <useProjectArtifact>true</useProjectArtifact>
            <useProjectAttachments>true</useProjectAttachments>
            <useStrictFiltering>true</useStrictFiltering>
            <includes>
                <include>${project.groupId}:${project.artifactId}:zip:${bonita.environment:lowercase}</include>
            </includes>
            <outputDirectory>
                BonitaSubscription-${branding.version}/server/webapps/bonita/WEB-INF/classes/my-application/
            </outputDirectory>
        </dependencySet>
        <!-- Copy bonita configuration archive into bundle classpath -->
        <dependencySet>
            <useProjectArtifact>true</useProjectArtifact>
            <useProjectAttachments>true</useProjectAttachments>
            <useStrictFiltering>false</useStrictFiltering>
            <includes>
                <include>${project.groupId}:${project.artifactId}:bconf:${bonita.environment:lowercase}</include>
            </includes>
            <outputDirectory>
                BonitaSubscription-${branding.version}/server/webapps/bonita/WEB-INF/classes/my-application/
            </outputDirectory>
        </dependencySet>
    </dependencySets>
</assembly>
