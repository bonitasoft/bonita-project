# The test does not execute docker locally, but only assert command line arguments are correctly set
invoker.name = Test Docker package
invoker.goals = package -ntp -Pdocker -Ddocker.buildArgs=--no-cache
invoker.buildResult = success
